<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-wEmeIV1mKuiNpC+IOBjI7aAzPcEZeedi5yW5f2yOq55WWLwNGmvvx4Um1vskeMj0" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header class="display-flex justify-space-around align-center p-2 header">
    <h1>
      The Tech Blog
    </h1>
    <p>Welcome {{userName}}!</p>
    <nav>
      <a href="/" class="text-decoration-none">Home</a>
      {{#if logged_in}}
      <a href="/myposts" class="text-decoration-none">My Posts</a>
      {{/if}}
      <!-- Conditionally render login or logout links -->
      {{#if logged_in}}
      {{!-- <a href="/profile">profile</a> | --}}
      <button class="no-button" id="logout">Log Out</button>
      {{else}}
      <a href="/login">login</a>
      {{/if}}
    </nav>

    {{!-- <p>{{ loggedInUser }}</p> --}}
  </header>
  <main class="container container-fluid mt-5">
    <!-- Render the contents of the sub layout here -->
    {{{ body }}}
  </main>
  
</body>
<footer></footer>
{{#if logged_in}}
  <script src="/js/logout.js"></script>
{{/if}}
</html>
